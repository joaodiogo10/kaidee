[package]
name = "kaidee-analysis"
version = "0.1.0"
edition = "2021"

[dependencies]
cpal = "0.15"
ringbuf = "0.4"
rustfft = "6"

[dev-dependencies]
symphonia = { version = "0.5", default-features = false, features = ["wav", "mp3", "pcm"] }
serde_json = "1"
